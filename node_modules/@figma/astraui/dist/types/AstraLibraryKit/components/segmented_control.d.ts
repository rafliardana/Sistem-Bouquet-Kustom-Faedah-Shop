import { ReactNode } from "react";
interface SegmentedControlProps {
    segments: Array<{
        id: string;
        icon: ReactNode;
    }>;
    selectedSegment: string;
    onChange: (segmentId: string) => void;
    className?: string;
}
export declare function SegmentedControl({ segments, selectedSegment, onChange, className, }: SegmentedControlProps): import("react/jsx-runtime").JSX.Element;
export {};
