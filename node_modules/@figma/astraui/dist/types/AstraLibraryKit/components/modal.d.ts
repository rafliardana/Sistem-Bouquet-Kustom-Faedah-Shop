import { ReactNode } from 'react';
interface ModalProps {
    isOpen: boolean;
    onClose: () => void;
    title?: string;
    children: ReactNode;
    footer?: ReactNode;
    size?: 'small' | 'medium' | 'large';
    className?: string;
}
export declare function Modal({ isOpen, onClose, title, children, footer, size, className }: ModalProps): import("react/jsx-runtime").JSX.Element | null;
export {};
